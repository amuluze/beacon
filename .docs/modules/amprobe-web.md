# Amprobe Web 模块

> Generated by `/sdd doc update` from local workspace facts.
> Do not hand-edit this file; update source files and rerun the command.

## 模块职责

`amprobe-web` 的主要角色是：frontend experience module: Vue/Vite 页面、路由、API client、状态管理和用户交互。

- 承载 Vue 页面、路由、API client、状态管理和用户可见交互。
- 相关源码包：`amprobe/web`。

## 职责边界

| 职责 | 说明 | 是否负责 |
|------|------|----------|
| 模块内实现 | frontend experience module: Vue/Vite 页面、路由、API client、状态管理和用户交互 | yes |
| 跨模块协调 | 仅通过公开接口或项目约定完成 | conditional |
| 其他模块内部状态 | 不直接读写其他模块私有实现 | no |

## 模块事实

- 前端包：`amprobe/web/package.json`
- package name：`amprobe-web`
- 前端源码文件：113
- scripts：`build`, `dev`, `lint`, `lint:fix`, `preview`, `ts`
- 前端信号：19 个
- 推断角色：frontend experience module: Vue/Vite 页面、路由、API client、状态管理和用户交互

## 接口与符号信号

本节记录源码扫描得到的导出符号或前端入口信号；它们是候选接口线索，不等同于跨模块公开 API。跨模块调用仍以 facade、模块文档和 Domain Spec 约束为准。

- `amprobe/web/src/api/index.ts`：axios/API 调用
- `amprobe/web/src/api`：API client
- `amprobe/web/src/components/Websocket/index.ts`：WebSocket 实时通道
- `amprobe/web/src/components`：共享组件
- `amprobe/web/src/hooks`：组合式逻辑/hooks
- `amprobe/web/src/interface`：前端接口类型
- `amprobe/web/src/router/dynamic.ts`：Vue Router 入口
- `amprobe/web/src/router/index.ts`：Vue Router 入口
- `amprobe/web/src/router/routes.ts`：Vue Router 入口
- `amprobe/web/src/router`：路由定义
- `amprobe/web/src/store/modules/app.ts`：Pinia/store 状态
- `amprobe/web/src/store/modules/echarts.ts`：Pinia/store 状态
- `amprobe/web/src/store/modules/permission.ts`：Pinia/store 状态
- `amprobe/web/src/store/modules/theme.ts`：Pinia/store 状态
- `amprobe/web/src/store/modules/user.ts`：Pinia/store 状态
- `amprobe/web/src/store/setup.ts`：Pinia/store 状态
- `amprobe/web/src/store`：前端状态管理
- `amprobe/web/src/views/container/container/components/ViewLog.vue`：WebSocket 实时通道
- `amprobe/web/src/views`：页面视图

## 依赖关系

- `@antfu/eslint-config` `^3.14.0`
- `@element-plus/icons-vue` `^2.3.1`
- `@iconify-json/ep` `^1.2.2`
- `@types/codemirror` `^5.60.15`
- `@types/lodash-es` `^4.17.12`
- `@types/node` `^20.11.30`
- `@unocss/eslint-plugin` `^65.4.2`
- `@vitejs/plugin-vue` `^5.2.1`
- `axios` `^1.7.9`
- `codemirror` `^5`
- `codemirror-editor-vue3` `^2.8.0`
- `echarts` `^5.5.0`
- `echarts-liquidfill` `^3.1.0`
- `element-plus` `^2.9.3`
- `eslint` `^9.18.0`
- `eslint-plugin-format` `^1.0.1`
- `lint-staged` `^15.4.0`
- `lodash-es` `^4.17.21`

## 配置与入口信号

- 前端：`amprobe/web/src/api/index.ts`：axios/API 调用
- 前端：`amprobe/web/src/api`：API client
- 前端：`amprobe/web/src/components/Websocket/index.ts`：WebSocket 实时通道
- 前端：`amprobe/web/src/components`：共享组件
- 前端：`amprobe/web/src/hooks`：组合式逻辑/hooks
- 前端：`amprobe/web/src/interface`：前端接口类型
- 前端：`amprobe/web/src/router/dynamic.ts`：Vue Router 入口
- 前端：`amprobe/web/src/router/index.ts`：Vue Router 入口
- 前端：`amprobe/web/src/router/routes.ts`：Vue Router 入口
- 前端：`amprobe/web/src/router`：路由定义
- 前端：`amprobe/web/src/store/modules/app.ts`：Pinia/store 状态
- 前端：`amprobe/web/src/store/modules/echarts.ts`：Pinia/store 状态
- 前端：`amprobe/web/src/store/modules/permission.ts`：Pinia/store 状态
- 前端：`amprobe/web/src/store/modules/theme.ts`：Pinia/store 状态
- 前端：`amprobe/web/src/store/modules/user.ts`：Pinia/store 状态
- 前端：`amprobe/web/src/store/setup.ts`：Pinia/store 状态
- 前端：`amprobe/web/src/store`：前端状态管理
- 前端：`amprobe/web/src/views/container/container/components/ViewLog.vue`：WebSocket 实时通道
- 前端：`amprobe/web/src/views`：页面视图

## 数据模型与状态

- 数据模型以源码结构、导出类型信号和测试断言为准；本节只记录静态发现结果。
- 前端状态以路由、store、API client 和组件交互为准；请在变更页面协作边界时补充状态集合。

## 使用注意事项

- 不跨越模块边界直接依赖内部路径或未导出符号。
- 修改跨模块公开 API、导出符号信号、配置或副作用边界时，同步更新本文件、根入口文档和相关 Domain Spec。
- 配置、凭据和外部副作用只记录语义边界，不记录真实敏感值。

## 验证命令

```bash
cd amprobe/web && npm run lint
cd amprobe/web && npm run ts
cd amprobe/web && npm run build
```
