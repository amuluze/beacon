services:
  beacon:
    build:
      context: .
      dockerfile: beacon/Dockerfile
    image: beacon:local
    container_name: beacon
    restart: unless-stopped
    ports:
      - "1443:80"
    volumes:
      - ./deploy/logs:/app/logs
