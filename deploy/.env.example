# Generate a unique value for each secret and inject it through the deployment environment.
# For example:
# openssl rand -hex 32
# Never commit generated values.
BEACON_AUTH_SIGNING_KEY=
BEACON_AGENT_INSTALL_TOKEN=
BEACON_CONTROL_JOIN_TOKEN=
