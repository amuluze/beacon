apiVersion: v1
kind: Service
metadata:
  name: amprobe-server
  namespace: amprobe
spec:
  selector:
    app: amprobe-server
  ports:
    - name: http
      port: 8000
      targetPort: 8000
    - name: control
      port: 17000
      targetPort: 17000
  type: ClusterIP
